{% extends 'base.html.twig' %}
{% block body %}
    {{ form_start(form) }}
    <p>Nombre de places encore disponibles : {{ remaining_places }}</p>
    {{ err }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
{% endblock %}
